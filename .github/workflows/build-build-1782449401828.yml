name: Build Flutter APK build-1782449401828

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: zulu
        java-version: 17
        
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: 3.22.0
        channel: stable
        
    - name: Download and Extract ZIP
      run: |
        echo "Downloading project..."
        wget -O project.zip "https://github.com/ObyMoods/webApk/releases/download/build-1782449401828/project.zip"
        echo "Extracting ZIP..."
        unzip -o project.zip -d project/
        echo "Project extracted!"
        ls -la project/
        
    - name: Check and Fix pubspec.yaml
      run: |
        echo "Checking pubspec.yaml..."
        cd project/
        if [ -f "pubspec.yaml" ]; then
          echo "✅ pubspec.yaml found!"
          echo "Current SDK version:"
          grep "sdk:" pubspec.yaml || echo "No SDK constraint found"
          
          # Fix SDK version to match Flutter 3.22.0 (Dart 3.4.0)
          sed -i 's/^( *)sdk: .*$/\1sdk: ">=3.0.0 <4.0.0"/' pubspec.yaml
          echo "Updated SDK constraint to: >=3.0.0 <4.0.0"
          
          echo "Updated pubspec.yaml:"
          cat pubspec.yaml | head -30
        else
          echo "❌ pubspec.yaml not found!"
          exit 1
        fi
        
    - name: Clean project
      run: |
        cd project/
        echo "Cleaning project..."
        rm -rf build/ .dart_tool/ .flutter-plugins .packages pubspec.lock 2>/dev/null || true
        echo "Clean complete!"
        
    - name: Get Flutter dependencies
      run: |
        cd project/
        echo "Getting dependencies..."
        flutter pub get
        echo "Dependencies installed!"
        
    - name: Build APK
      run: |
        cd project/
        echo "Building APK..."
        flutter build apk --release
        echo "Build complete!"
        
    - name: Verify APK
      run: |
        echo "Verifying APK..."
        APK_PATH="project/build/app/outputs/flutter-apk/app-release.apk"
        if [ -f "$APK_PATH" ]; then
          echo "✅ APK found!"
          ls -la "$APK_PATH"
          file_size=$(du -h "$APK_PATH" | cut -f1)
          echo "📦 APK size: $file_size"
        else
          echo "❌ APK not found!"
          echo "Searching for APK files:"
          find project/ -name "*.apk" -type f 2>/dev/null || echo "No APK found"
          exit 1
        fi
        
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: app-release
        path: project/build/app/outputs/flutter-apk/app-release.apk
        retention-days: 7
    